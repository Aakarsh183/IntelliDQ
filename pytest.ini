[pytest]
# backend/ is not a package - main_app.py uses flat imports (`from grok_client import ...`),
# so backend must be on sys.path for anything under backend/tests to import it.
pythonpath = backend
testpaths = backend/tests
addopts = -q --strict-markers
